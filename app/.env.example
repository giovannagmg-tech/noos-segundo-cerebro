VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# OAuth Client ID do Google (não é segredo — é o identificador público do
# app OAuth; o Client Secret fica só no secret GOOGLE_CLIENT_SECRET do
# Supabase, nunca aqui). Criado em console.cloud.google.com/apis/credentials.
VITE_GOOGLE_CLIENT_ID=

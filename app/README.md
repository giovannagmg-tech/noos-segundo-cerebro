# Noos — app

Frontend do Noos: Vite + React + TypeScript + Tailwind v4 + shadcn/ui (Radix).

Ver o `README.md` da raiz do repositório e `SKILL.md` / `docs/` antes de mexer aqui.

```bash
cp .env.example .env   # preencher com Project URL + anon key do Supabase
npm install
npm run dev
```
